# In order to update R to the latest version, you have to reinstall. See the Learn_R User Guide. 

# I need the following packages (to be updated periodically)

```{r echo = true}
install.packages("tidyverse")
install.packages("ggplot2")
install.packages("shiny")
```

